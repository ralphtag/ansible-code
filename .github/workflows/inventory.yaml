on:
  workflow_dispatch:

jobs:
  generate-inventory:
    runs-on: [ ansible-master ]

    steps:
     

      - name: Create inventory directory
        run: |
          sudo -u ralph mkdir -p /home/ralph/ansible/
          sudo chown -R ralph:ralph /home/ralph/ansible

      - name: Generate inventory.yaml
        run: |
          sudo -u ralph bash -c 'cat > /home/ralph/ansible/inventory.yaml' << 'EOF'
          ---
          all:
            children:
              webservers:
                hosts:
                  web01:
                    ansible_host: 10.128.0.4
                    ansible_user: ralph
                    

              databases:
                hosts:
                  db01:
                    ansible_host: 10.204.0.2
                    ansible_user: ralph
                    db_version: 14
                   

              
          EOF
